# RULES — Mobile feature: containers

- Chỉ xử lý UX/tác nghiệp Mobile của `containers`.
- Không duplicate business rule từ Backend.
- Action quan trọng phải online và nhận kết quả xác nhận từ Backend.
- File API chỉ gọi ICD Backend.
